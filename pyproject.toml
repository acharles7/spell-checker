[tool.black]
line-length = 110
target_version = ['py38']
include = '\.pyi?$'
exclude = '''

(
  /(
      \.eggs         # exclude a few common directories in the
    | \.git/         # root of the project
    | \.mypy_cache/
    | \.egg-info
    | build
    | __pycache__
  )
)
'''
